import React from "react";
import { AppBar } from "@react-native-material/core";

const HeaderProject = ({ title }) => <AppBar style={{backgroundColor:"#AC3A41"}} centerTitle={true} title={title} />;

export default HeaderProject;